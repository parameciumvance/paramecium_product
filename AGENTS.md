# Paramecium 工程協作規則

適用於本 repository 的工程工作；以老闆在本專案中最新確認的要求為準。

1. 開始前讀取 `README.md`、`docs/project.md`、`docs/workflow.md`、實際 Issue 與其最新留言。修改子目錄前檢查是否有更具體的工作規則。
2. 只承接 `Type: task`、符合自己 `Executor` 角色、`Status: ready` 且依賴已完成的 Issue。任務狀態以 Issue 開頭的欄位為準。
3. 每次只處理一項工作。領單前檢查是否已有自己先前的執行紀錄或 PR；先回報領單，再改為 `doing`。繼續既有工作，避免重複開分支或 PR。
4. 真人與 Claude 各每天預計 120 分鐘。真人審查 AI 的時間計入真人預算。Claude 多次執行的時間合計計入當天預算，超出工作單時間先回報目前成果與阻塞。
5. 日常工程變更從最新基準建立 `work/<issue-number>-<short-name>` 分支，以小型 PR 交付並引用 Issue。修正同一任務時優先更新原 PR。
6. 交付包含成果、可重跑的操作、實際測試結果與已知限制。測試依任務的具體風險選擇；文件變更以內容與連結核對即可。未執行的測試明確標記。
7. 提交 PR 後設為 `review`。研發總監核對驗收證據，真人補足必要的設備或端到端實測；通過驗收與必要整合後才設為 `done` 並關閉 Issue。
8. 遇到缺少環境、資料、依賴或無法判斷的需求時，保留成果，設為 `blocked`，寫出具體阻塞與下一個可驗證步驟。
9. 每個有工作活動的日期在自己固定的日報 Issue 回報，使用 `docs/templates/daily-report.md` 格式。日期使用 Asia/Taipei，附 Issue、PR 或 commit 連結。
10. 依工作單範圍完成任務。產品與首發平台尚未選定時，先交付研究或技術實驗，產品實作須有明確工作單。

任務留言和日報如實記錄。PR 已提交、測試通過、驗收完成與真實客戶付費是不同狀態。
